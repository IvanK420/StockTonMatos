{% extends 'base.html.twig' %}

{% block title %}Moulin index{% endblock %}

{% block body %}
    <h1>Moulin index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Marque</th>
                <th>Nom</th>
                <th>Taille</th>
                <th>Poids</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for moulin in moulins %}
            <tr>
                <td>{{ moulin.id }}</td>
                <td>{{ moulin.Marque }}</td>
                <td>{{ moulin.Nom }}</td>
                <td>{{ moulin.taille }}</td>
                <td>{{ moulin.poids }}</td>
                <td>
                    <a href="{{ path('app_moulin_show', {'id': moulin.id}) }}">show</a>
                    <a href="{{ path('app_moulin_edit', {'id': moulin.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_moulin_new') }}">Create new</a>
{% endblock %}
