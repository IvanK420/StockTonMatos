{% extends 'base.html.twig' %}

{% block title %}Fil{% endblock %}

{% block body %}
    <h1>Fil</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ fil.id }}</td>
            </tr>
            <tr>
                <th>Marque</th>
                <td>{{ fil.Marque }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ fil.Nom }}</td>
            </tr>
            <tr>
                <th>Taille</th>
                <td>{{ fil.taille }}</td>
            </tr>
            <tr>
                <th>Resistance</th>
                <td>{{ fil.resistance }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ fil.quantite }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_fil_index') }}">back to list</a>

    <a href="{{ path('app_fil_edit', {'id': fil.id}) }}">edit</a>

    {{ include('fil/_delete_form.html.twig') }}
{% endblock %}
